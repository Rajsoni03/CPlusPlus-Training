#include <iostream>
using namespace std;

int main(){
  int a, b;
  // int a = 10, b = 20;
  // a = 10, b = 20;
  // a = 10, b = a + 20;  
  // a = b + 10, b = 20;  
  // int a = 10, b = 20 + a;
  // int a = b + 10, b = 20;

  a = 10, 20; // (a = 10), 20;
  b = (10, 20);

  cout << a << "  " << b << endl;

  return 0;
}